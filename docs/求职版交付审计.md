# NovelSim 求职版交付审计

审计时间：2026-07-30。判定原则：代码存在不等于交付完成；必须有可运行入口、
固定证据或真人验收。未测项不按通过处理。

## 1. 完成定义对照

| 交付项 | 状态 | 证据 / 下一动作 |
|---|---|---|
| Windows x64 可执行包 | 已完成（本地） | `unity/NovelSim3D/Builds/Windows/NovelSim3D.exe`；当前脚本重新实跑 smoke 通过 |
| 后端 + Unity 一键启动 | 已完成 | `start-unity-demo.cmd`，先验证 API 元数据健康，再启动缺失的后端和可见 Unity 客户端 |
| 2–3 分钟无剪辑核心视频 | 已完成（本地） | `portfolio/video/NovelSim-core-demo-v1.mp4`；真实 Unity/HTTP 连续录制 138.50 秒 |
| 10–15 分钟演示脚本 | 已完成 | 同上，含时间轴、命令、口播、失败演示和常见追问 |
| 项目架构图 | 已完成 | `docs/作品集架构与因果图.md` Mermaid |
| Agent 状态机图 | 已完成 | 同上 |
| 信息传播/联盟因果图 | 已完成 | 同上，包含 belief 来源、父证据和联盟前置条件 |
| 消融与失败样本 | 已完成 | G0–G3 `6/4/1/0`、无记忆/FTS5、两条真实 `INVALID_ACTION` |
| 招聘方 README 首屏 | 已完成 | 七问静态门禁覆盖 |
| 原创公开世界包 | 已完成 | `portfolio/worlds/secret-letter-v1.json` + `.sha256` |
| 可复现命令与环境 | 已完成 | README、评测文档、世界包导出/校验、Unity build/smoke |
| Pairwise 真人校准 | 已完成 | 6 题真人盲标已写入 `evaluation/human_labels_strong_v1.jsonl`；离线校准一致率 83.33%、Cohen's κ 0.667，新增模型调用 0 |

因此当前状态是：**求职版收口项均已验收**。人工标签来自用户本人选择，不是
复制 Judge 输出；主观一致性只用于评估偏好，不能覆盖世界规则等客观门禁。

## 2. 当前硬证据

- Python：`333 passed, 15 deselected`；
- 确定性场景：9/9，核心事件、合法工具、预期拒绝、回放、传播和证据链
  均为 100%；
- 权威安全：非法 Patch、未知实体接受、因果越权提交、认知泄漏均为 0；
- 真实 LLM：20/20 完整，18/20 目标成功，45 次调用、49,255 Token；
- 真实 LLM 延迟：P50/P95 `10.554s/16.469s`；
- 已提交事件：回放、因果证据、叙事覆盖和结构化事件依据均为 100%；
- 强基线 Pairwise：6/6 Judge 成功、0 解析/调用失败、最终 3–3；
- 真人校准：6 个标签，Judge/真人一致 5/6，一致率 `83.33%`，
  Cohen's κ `0.667`，离线回填新增 LLM 调用 `0`；
- Unity：EditMode `6/6`、PlayMode `7/7`；
- Windows smoke：真实 E 交互推进到 v1、消费 5 条表现命令，重启恢复同一
  `session_id`，最终表现游标 1999；
- Windows 三路线 smoke：销毁密信 `letter_destroyed` v2、携信离开
  `player_intercepted` v2、公开真相 `truth_exposed` v5；三者均经过真实
  Unity → HTTP → SQLite，并由另一个客户端进程恢复相同 session/version/游标；
- Unity 成片：H.264、1024×576、约 30 FPS、138.50 秒、整段 FFmpeg 解码通过；
  运行报告为 `passed`，session `54a4ebac235287e4`，最终 v1，
  `WORLD_CONCEPT_UNAVAILABLE`，表现命令 6 条、游标 1999；
- 成片 SHA-256：
  `692605fc1a1cb89bcf70c1cf3841f5c1d9bd283bc71edbc1770337f02986ff26`；
- 原创世界包 SHA-256：
  `be328a02de27c6e7c74d3099b6b5e455a56e54e3b67c183aa3ab6c8cd55f6748`。

## 3. 真人盲标校准结果

按盲测包题号顺序，真人选择为：

```text
Left, Right, Right, Left, Left, Left
```

标签已保存到 `evaluation/human_labels_strong_v1.jsonl`。以下命令离线回填已有
Judge 报告，不会再次调用 LLM：

```powershell
.\.venv\Scripts\python.exe -m evaluation.pairwise_calibration `
  --report evaluation\reports\pairwise-strong-v1.json `
  --labels evaluation\human_labels_strong_v1.jsonl `
  --json evaluation\reports\pairwise-strong-calibrated-v1.json `
  --markdown evaluation\reports\pairwise-strong-calibrated-v1.md
```

实测输出：`human_labels=6`、`agreement=0.833333`、
`cohen_kappa=0.666667`、`llm_calls_added=0`。唯一分歧是
`pair_pair_walk_02`；这不是“答错”，而是两位评审对文本偏好的不同。样本只有
6 题，因此只能作为小样本校准，不能外推为总体质量结论。

## 4. 视频验收

`unity/NovelSim3D/record-showcase.ps1` 已自动完成并验证：

- 真实 Windows Unity 窗口区域连续录制，没有静态图片拼接；
- 2:18.50，位于 2:00–3:00 范围；
- 真实交互将权威状态 v0 推进到 v1，Unity 消费表现命令；
- “夜轻歌开飞机飞走了”返回 `WORLD_CONCEPT_UNAVAILABLE`，不产生 Patch、
  版本或飞行动画；
- 恢复同一 session 后仍为 v1；
- 结构化 JSON 报告为 `passed`，MP4 整段解码与 SHA-256 检查通过；
- 开场、提交、拒绝、结果四个阶段已抽帧检查，未展示 `.env`、API Key、
  私有小说全文或账户信息。

成片使用画面内字幕，无音轨。投递前可再由本人完整观看一次，并将仓库相对链接
替换为公开发布链接；这不阻塞本地交付物的完成状态。

## 5. 明确后续范围

A/B/C/D 长程真实模型消融移入作品集后续，不阻塞当前求职版。当前已经交付：

- G0–G3 世界规则/能力/因果/叙事门禁消融；
- no-memory 与 SQLite FTS5 记忆消融；
- 同模型、同权威上下文 direct-prompt 强基线；
- 20 局真实模型固定回归。

后续若扩展 A/B/C/D，必须固定模型、Prompt 预算、随机种子、场景和价格快照，
并把真实调用费用作为独立实验显式批准。
