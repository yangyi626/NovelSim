# 实现进度

> 本文档记录 AI 快穿系统的实际实现进度，与 [`plan.md`](plan.md)（设计规划）、[`AI快穿系统_模块作用功能技术对应表.md`](AI快穿系统_模块作用功能技术对应表.md)（模块详表）对照。
>
> - `plan.md` 是最初的总体蓝图；当前采用的实现基线以本文档为准。
> - 本文档是"已经做到哪、还差什么"的现实快照，随实现推进更新。
>
> 最近更新（2026-07-28）：首轮双书 quick 真实 LLM 编译演练完成；修复关系实体类型与断点缓存键，高严重度质量问题现会阻止自动审核

---

## 0. 一句话现状

**核心 AI 引擎 + 角色 Agent + 小说编译器 + 可持久化 Web 试玩闭环已经完成并验证，创作者后台已具备账户分权和审计，生产数据架构已形成。** 玩家可用自然语言改变世界并触发 NPC 自主反应；NPC 会召回自己的相关经历，并从多次经历中形成带证据链、可被后续认知纠正的反思。当前正式开发组合是 SQLite 权威状态/事件/记忆/账户/任务 + Qdrant Local Mode 派生向量索引，不需要 Docker 或独立数据库服务；全书编译由独立 Worker 通过 SQLite 租约执行，PostgreSQL + pgvector 后端代码继续保留。项目已提供 Windows 一键启动和跨平台进程管理入口，并在 Pull Request 与 `main` 推送时执行确定性 CI 门禁。

里程碑：plan.md 第七、八步（LLM + 完整 Turn Pipeline）+ 第十步（角色 Agent）+ 第十二步（小说世界编译器 A/B/C/D 阶段）全部跑通；在此基础上已增加 FastAPI 接入层、Vue 3 试玩界面和创作者后台 MVP+。

---

## 1. 已完成的核心能力

### 1.1 世界数据标准（world_schema）

对应 plan 第三节。定义了整个系统的"南北极"数据契约：

| Schema | 作用 | 状态 |
|---|---|---|
| `Action` | 玩家/NPC 想做什么（含 ActionType 白名单） | ✅ |
| `WorldState` | 当前客观世界事实（权威状态快照） | ✅ |
| `StatePatch` | 本轮允许的状态变化（含 Operation 白名单） | ✅ |
| `WorldEvent` | 已正式发生的不可变事实（事件溯源基石） | ✅ |
| `CharacterBelief` | 角色认知（含 keywords 支持中文泄漏检测） | ✅ |
| `CharacterRelation` | 多维关系（affection/trust/fear/hostility/respect/debt） | ✅ |
| `NarrativeOutput` / `DialogueLine` | 表现层数据契约（旁白/对白/系统提示） | ✅ |
| `Operation` | 状态操作（13 种 op，`extra="forbid"` 严格受控） | ✅ |
| `Rule` / `WorldRule` / `PlotArc` / `Character` / `Item` / `Location` | 世界实体 | ✅ |

设计要点：
- 所有实体 `extra="allow"`，小说特有字段（如 `cultivation_level`）放 `attrs` 自由口袋。
- `Operation` 严格 `extra="forbid"`——这是"LLM 可提议、但不可越权改库"的权限边界。

### 1.2 世界运行引擎与持久化（engine）

对应 plan 第六、七、八节。状态推演保持内存纯函数语义；正式提交后的世界状态、事件日志、回合表现与角色记忆通过统一 `WorldStore` 契约写入 SQLite 或 PostgreSQL。

| 模块 | 作用 | 状态 |
|---|---|---|
| `engine/patch.py` | `apply_patch` 纯函数：StatePatch → 新 WorldState（深拷贝、不修改输入、数值钳制） | ✅ |
| `engine/event.py` | `commit_event`（乐观锁）+ `replay_events`（事件回放）+ `state_hash`（一致性断言） | ✅ |
| `engine/rules.py` | 确定性规则引擎：时空/物品/身份/认知边界校验 | ✅ |
| `engine/patch_validator.py` | 应用前校验层：拦编造实体/数值越界/非法维度，违规可反馈 LLM 重试 | ✅ |
| `engine/narrative_consistency.py` | 叙事一致性审查：死人不说话、认知不泄漏 | ✅ |
| `engine/character_agent.py` | **角色 Agent**：单个 NPC 自主决策（人格/目标/计划/情绪 → 候选动作 + patch） | ✅ |
| `engine/agent_scheduler.py` | **NPC 调度器**：事件驱动选谁行动 + 合并多 NPC patch | ✅ |
| `engine/persistence.py` | SQLite 世界线/状态/事件/回合存储；角色长期记忆、FTS5 中文检索、兼容迁移、事务提交、重启恢复、持久化乐观锁 | ✅ |
| `engine/qdrant_memory.py` | Qdrant Local/Server 向量索引、SQLite 权威回查、FTS5/语义混合重排、删除/裁剪同步与自动降级 | ✅ |
| `engine/memory_retrieval_eval.py` / `evaluate_memory_retrieval.py` | Hit@K、Recall、MRR、nDCG、无关率、延迟评测；FTS5 基线与多权重自动对照门禁 | ✅ |
| `engine/postgres_persistence.py` | PostgreSQL JSONB 权威状态、行锁乐观并发、GIN 全文检索、pgvector HNSW 与混合重排 | 🟡 代码完成，待目标数据库契约测试 |
| `engine/storage.py` / `store_factory.py` | 权威存储统一契约；默认 SQLite，按配置叠加 Qdrant 或切换 PostgreSQL | ✅ |
| `engine/memory_projection.py` | 事件→情景记忆投影、幂等重建、每角色容量裁剪 | ✅ |
| `engine/reflection_memory.py` | 多情景→反思记忆；证据链、稳定主张 ID、幂等更新、写入/召回双重认知冲突保护 | ✅ |
| `engine/llm_trajectory_eval.py` / `evaluate_llm_trajectory.py` | 真实 LLM 长轨迹分块评分、总体聚合与发布门禁 | ✅ |
| `engine/rebuild_qdrant.py` | 从权威记忆表按单世界线或全量重建 Qdrant 派生索引 | ✅ |
| `engine/rebuild_reflections.py` | 按单世界线或全量离线生成/重建反思记忆 | ✅ |
| `engine/trajectory_eval.py` | 长轨迹版本链、实体引用、死亡角色、Patch 与终态一致性发布门禁 | ✅ |
| `engine/migrate_storage.py` | SQLite 全存档迁移 PostgreSQL，并重建派生记忆 | 🟡 已实现，待真实 PostgreSQL 演练 |

### 1.3 三个 LLM 模块（对应 plan 第七步）

这是让世界"活起来"的核心。当前默认接入 qwen3.7-plus（DashScope OpenAI兼容端点）。

| 模块 | LLM 任务 | 作用 | 状态 |
|---|---|---|---|
| `engine/action_parser.py` | 任务1：Action Parser | 自然语言 + WorldState → 结构化 Action | ✅ |
| `engine/transition.py` | 任务2：State Transition | Action + WorldState → 候选 StatePatch（智能推演后果） | ✅ |
| `engine/narrative.py` | 任务3：Narrative Generation | 已提交事件 + 状态 → 旁白/对白/系统提示 | ✅ |

每个模块都有：JSON 容错提取、Schema 校验、失败重试、安全兜底。

### 1.4 完整 Turn Pipeline

对应 plan 第八节。`engine/turn.py` 把上述组件串成一条完整游戏回合：

```
用户自然语言
   ↓ ActionParser (LLM)
Action
   ↓ RuleEngine (规则校验) ──┐
合法 Action                  │ 三层安全防线
   ↓ TransitionProposer (LLM)│ - Pydantic Schema
StatePatch                   │ - patch_validator
   ↓ CharacterScheduler ★   │ - apply_patch 兜底
NPC 自主反应叠加              │
   ↓ commit_event            │
WorldEvent (version+1)       │ - 死人不说话
   ↓ NarrativeGenerator (LLM)│ - 认知不泄漏
NarrativeOutput              │
   ↓
玩家看到的: 旁白 + 对白 + 系统提示 (+ NPC 的对白/动作)
```

★ 角色调度默认关闭 (`use_npc_agents=False`)，显式开启后玩家行动提交前会唤醒相关 NPC。

### 1.5 角色 Agent（plan 第十步）

让 NPC 有自主行动，而不是只对玩家行动做"反应式 patch"。这是世界"活起来"的关键。

| 模块 | 作用 |
|---|---|
| `world_schema.CharacterPsyche` | 角色内在状态：人格 traits / 目标 goals / 计划 plans / 情绪 emotion / 短期记忆 |
| `world_schema.AgentDecision` | 一次决策产物：候选动作 + 预期 patch + 情绪/感知更新 |
| `engine.CharacterAgent` | 单个 NPC 的决策单元：读取**自己可见的事实**（认知不越权）→ LLM 生成候选动作 → patch_validator 兜底 |
| `engine.CharacterScheduler` | 事件驱动调度：选哪些 NPC 行动（被波及/在场/有计划者）→ 合并 patch |

决策流程（plan 第八节）：
```
读取角色可见事实 (基于 beliefs)
→ 检索角色相关记忆 (recent_perceptions)
→ 生成候选动作 (LLM，产出 AgentDecision)
→ 规则过滤 + patch_validator 兜底
→ 选择动作 (可按兵不动)
→ 产出 NPC Action + 候选 StatePatch
```

华容巷 psyche 初始化（`examples/huarong_lane/scenario.py.build_psyches`）：
- **夜轻歌**（玩家宿主，is_player=True，永不被自动调度）：目标"反客为主洗刷废柴名"，计划"先立威→查真相→夺主导权"
- **夜清清**（自主 NPC）：人格"阴毒/隐忍/心机深"，目标"除掉夜轻歌上位嫁小王爷"，计划"陷害→联合管家→取代嫡位"
- **林管家**（自主 NPC）：人格"圆滑/趋炎附势/与清清有私情"，目标"保全夜清清维护自身地位"

#### 长期记忆的第二层：反思/语义记忆

- 情景记忆继续记录 NPC 亲历的具体事件。
- 每 5 个世界版本检查一次；至少 3 条未处理经历才调用低温 LLM。
- 每条反思至少引用 2 个事件 ID，并保存稳定 `fact_id`、belief 与 confidence。
- 相同主张使用稳定来源 ID 幂等更新，并合并新增证据，不堆积同义记忆。
- 写入前拒绝与当前权威认知相反的候选；召回前再次过滤后来已经过期的反思。
- 反思完整记录保存在 SQLite，Qdrant 只同步可重建向量与过滤元数据。
- 首次空结果会进行一次受约束的二次审视；真实模型已成功从“折断刀刃 + 护卫畏惧”提炼“夜轻歌可能隐藏实力”。
- 独立语义审查器再次检查证据蕴含、矛盾、覆盖率和过度推断；默认总分门槛 0.72，失败关闭。

详细设计与运维见 [`反思与语义记忆.md`](反思与语义记忆.md)。

### 1.6 小说世界编译器（plan 第十二步）

用 LLM 把整本 TXT 自动编译成 WorldPackage，不再手工造。

| 模块 | 作用 | 是否 LLM |
|---|---|---|
| `compiler/text_loader.py` | TXT 导入（自动编码探测）→ 文本清洗 → 章节切分 → 场景切分（启发式） | 否（确定性） |
| `compiler/extractors.py` | `EntityExtractor`：场景文本 → 实体/关系/事件/规则/角色状态/伏笔/目标演化（带 evidence + confidence） | 是 |
| `compiler/scene_compiler.py` | `EntityRegistry` + `SceneCompiler` + `ChapterCompiler` + `VolumeCompiler` + `PackageBuilder` | 否 |
| `compiler/book_compiler.py` | 全书级身份消歧、多时间线、章节/卷/全书三级快照与评分事件投影 | 否 |
| `compiler/job_store.py` / `job_runner.py` / `worker.py` | SQLite 租约队列、独立 Worker、逐章进度/场景缓存/快照，暂停继续取消、重启恢复、自动质量审核 | 编排层调用 LLM |
| `compiler/cli.py` | CLI 入口：`python -m compiler.cli novels/xxx.txt -c 1 2 -o worlds/pkg.json` | 是 |

编译流水线：
```
TXT → 编码探测 → 清洗 → 章节切分 → 场景切分
   → EntityExtractor (LLM)：实体/关系/事件/规则/跨章演化
   → EntityRegistry：alias 消歧 (跨场景/章节 alias → 稳定 id)
   → VolumeCompiler：角色章节状态 + 伏笔生命周期 + 目标演化
   → 事件草稿 op → 严格 Operation (patch_validator 校验)
   → 累积进 WorldState
   → 发布 WorldPackage (JSON + 可直接 build_snapshot)
```

每条抽取结果带 `evidence`（原文片段）+ `confidence`（0-1），便于人工审核与回归测试（plan 第十五步）。

当前完成 plan 第十二步的 **A（单场景）+ B（单章节）+ C（单卷跨章节演化）+ D（全书全局消歧、多时间线、分层快照）**。全书编译通过 SQLite 任务运行，支持场景缓存、暂停、继续、取消和服务重启后的断点恢复。

### 1.7 测试世界包（examples/huarong_lane）

对应 plan 第四节。取材《第一狂妃》第 1-2 章"华容巷"手工建模：
- 4 个角色（夜轻歌/夜清清/林管家/夜青天）+ 1 个物品（外衫）+ 4 条关系
- 3 条世界规则（嫡庶尊卑、丹田体系、婚约）+ 角色认知初始化
- **3 个角色 psyche**（夜轻歌玩家 + 夜清清/林管家自主 NPC）
- 介入锚点：夜轻歌被诬通奸、当众受辱

### 1.8 Web 试玩界面（FastAPI + Vue 3）

在核心引擎之上增加了可直接游玩的浏览器界面：

| 模块 | 作用 | 状态 |
|---|---|---|
| `web/app.py` | FastAPI 应用；提供试玩、存档与创作者世界包 API，并托管前端产物 | ✅ |
| `web/run.py` | Uvicorn 启动入口；自动定位项目根目录，支持从根目录或 `web/` 目录运行 | ✅ |
| `web/frontend/src/App.vue` | 页面状态与回合流程编排 | ✅ |
| `StoryFeed.vue` | 展示玩家输入、旁白、对白、系统提示和 NPC 自主反应 | ✅ |
| `StatePanel.vue` | 展示世界时间、场景、人物心理、目标、计划和多维关系 | ✅ |
| `TurnInput.vue` | 自然语言回合输入 + NPC 自主反应开关 | ✅ |
| `SaveManager.vue` | 多世界线存档列表、新建、改名、载入、导入、导出和删除 | ✅ |
| `CreatorStudio.vue` | 世界包版本；角色/心理/认知/地点/物品/关系/规则/剧情线编辑；校验、保存和试玩 | ✅ |
| `RelationshipGraph.vue` | SVG 有向人物关系图，按信任、敌意、畏惧等维度着色 | ✅ |

当前 API：

- `POST /api/start`：从指定 `package_id` 创建干净世界快照并返回会话 ID。
- `GET /api/session?session=<id>`：恢复已有会话，返回当前状态和世界元信息。
- `POST /api/turn`：运行完整 Turn Pipeline，成功后把新状态写回当前会话。
- `GET /api/saves`：列出全部世界线存档及其版本、更新时间。
- `PATCH /api/saves/<id>`：修改存档名。
- `GET /api/saves/<id>/export`：下载包含状态、事件和剧情历史的完整 JSON 备份。
- `POST /api/saves/import`：校验并原子导入完整备份，以新会话 ID 创建世界线。
- `DELETE /api/saves/<id>`：删除世界线及其事件和剧情历史。
- `GET /api/state?session=<id>`：查询会话状态，用于调试。
- `GET /api/events?session=<id>`：按版本顺序查询事件日志，用于审计与回放。
- `GET /api/creator/packages` / `GET /api/creator/packages/<id>`：列出/读取世界包。
- `POST /api/creator/packages/<id>/clone`：把内置或已有世界包另存为新版本。
- `POST /api/creator/packages/validate`：执行 Schema 与跨实体引用校验。
- `PUT /api/creator/packages/<id>`：使用修订号冲突保护保存世界包。
- `GET /api/creator/packages/<id>/revisions`：读取不可变修订历史。
- `GET /api/creator/packages/<id>/diff`：比较两个修订的结构化字段差异。
- `POST /api/creator/packages/<id>/review`：执行提交审核、批准、驳回与发布状态转换。
- `GET /`：托管 Vite 构建出的单页应用。

当前边界：

- 内置 `examples/huarong_lane` 作为只读基准；支持从 `worlds/*.json` 载入创作者版本和编译器产物。
- 会话状态与事件日志保存在 `data/world.sqlite3`；浏览器用 `localStorage` 保存当前会话 ID 并自动恢复。
- 支持多个独立世界线的创建、改名、载入、导入、导出和删除；每个存档同时恢复玩家输入、旁白、对白、系统提示和 NPC 反应卡片。
- 导入时验证格式版本、世界状态 Schema、事件版本链和剧情历史序号；写入失败不会留下半份存档，也不会覆盖原世界线。
- 创作者账户与 RBAC 已完成；玩家存档仍无云同步，团队/项目级多租户隔离尚未实现。
- 前端默认开启 NPC 自主反应；每回合需要真实 LLM 调用。
- 已覆盖 Web 会话创建、回合持久化、恢复、创作者修订/审核 API；关系图和审核状态已通过真实浏览器验收。

### 1.9 创作者后台 MVP+（plan 第十三步）

本阶段已形成“模板创作 → 校验 → 发布本地版本 → 新开局试玩”的最小闭环：

- `engine/world_packages.py`：统一管理内置只读包与 `worlds/*.json` 创作者包。
- 内置包不可原地修改，必须克隆为 `<package_id>_vN`，避免破坏回归基准。
- 保存时校验 `WorldState` Schema、默认角色、当前场景、角色/物品/地点引用、地点层级循环、物品双重归属、关系端点、认知与心理角色、重复认知事实、认知来源/关键词、Agent 目标/计划引用、规则 ID。
- 使用 `revision` 乐观锁阻止旧编辑页面覆盖新修订。
- 每次保存和审核操作写入 `worlds/.history/` 不可变修订，可查看最近版本并生成结构化差异。
- 审核状态机为 `draft → pending_review → approved → published`，支持驳回、撤回和发布后开启新修订。
- 兼容 `compiler.PackageBuilder` 的既有输出格式，可直接载入创作台补充场景名、介入锚点和默认玩家角色。
- 创作台提供总览、角色、角色心理、角色认知、地点、物品、关系、世界规则、剧情线和高级 JSON 十个编辑区。
- 地点编辑支持父子层级、访问权限和扩展属性；删除前检查初始场景、角色、物品及子地点引用。
- 物品编辑支持角色持有/地点放置、数量、唯一性、可交互性和扩展属性。
- 角色心理编辑支持人格、情绪、玩家宿主标记、短期记忆、目标优先级与计划步骤。
- 角色认知编辑支持五级事实判断、可信度、信息来源、来源事件和中文关键词；用于约束 NPC 决策与对白认知泄漏。
- “保存并试玩”会保存当前修订，再调用 `/api/start` 从该模板创建独立 SQLite 世界线。
- 人物关系编辑区包含 SVG 有向关系图，审核面板展示状态、修订历史和前后差异。
- 全书编译页展示任务和逐章进度、缓存命中、章节/卷/全书快照、质量评分，并提供暂停、继续和取消。
- 编译完成后自动执行真实 LLM 长轨迹门禁；通过后进入 `pending_review`，失败或异常时保留草稿供修正。
- 创作者账户使用 Bearer token；`creator`、`reviewer`、`publisher`、`admin` 分离编辑、审核、发布和用户管理权限。
- 关键创作、编译、审核、发布和权限拒绝操作写入 SQLite 审计事件；自动质量门禁以 `system` 身份记录。
- 核心 API 已冻结为 `1.0.0`，契约清单由自动测试防止路径和方法被意外删除。

---

## 2. 安全边界（已确立并测试）

这是整个系统能长期稳定运行的根基，对应 plan 第七节"LLM 只开放受控权限"：

```
LLM 可以提议 Action
LLM 可以提议 StatePatch
LLM 可以生成表现文本

LLM 不可以直接执行 SQL
LLM 不可以绕过规则
LLM 不可以直接修改权威状态
```

具体落地的三层防线：

| 层 | 机制 | 拦截案例 |
|---|---|---|
| 第1层 | Pydantic Schema（`Operation extra="forbid"`） | LLM 输出非法字段被拒 |
| 第2层 | `patch_validator`（应用前校验） | 编造 item_id、`delta=5.0` 越界、`dimension="love"` 非法维度 |
| 第3层 | `apply_patch` 内置校验（应用时兜底） | 实体不存在、数值超界 |
| 表现层 | `narrative_consistency` | 死人说话、对白泄露未知事实 |
| 角色层 | `CharacterAgent` 认知隔离 + psyche 校验 | NPC 只依据自己 beliefs 行动；`update_psyche` 不可作用于玩家宿主；死人/玩家不调度 |
| 编译层 | `draft_ops_to_patch` + `patch_validator` | 编译抽取的事件草稿 op 引用未知实体时被丢弃，不污染 WorldState |

实测全部防线有效，有对应测试覆盖。

---

## 3. 项目结构

```
35_快穿系统/
├── world_schema/              # 世界数据标准
│   ├── models.py             # 全部 Schema (Action/State/Patch/Event/Narrative...)
│   └── __init__.py
├── engine/                    # 世界运行引擎、持久化、记忆与质量评估
│   ├── patch.py              # apply_patch 纯函数
│   ├── event.py              # 事件提交 + 回放 + 乐观锁
│   ├── rules.py              # 确定性规则引擎
│   ├── patch_validator.py    # 应用前校验层
│   ├── action_parser.py      # LLM 任务1: 自然语言 → Action
│   ├── transition.py         # LLM 任务2: Action → StatePatch
│   ├── narrative.py          # LLM 任务3: 事件 → 叙事
│   ├── narrative_consistency.py  # 叙事一致性审查
│   ├── character_agent.py    # ★ 角色 Agent: NPC 自主决策
│   ├── agent_scheduler.py    # ★ NPC 调度器 + patch 合并
│   ├── turn.py               # 完整 Turn Pipeline 编排 (含 NPC 反应)
│   ├── persistence.py        # ★ SQLite 世界线/状态/事件/回合存储
│   ├── config.py             # .env 配置加载 + 代理绕过
│   └── _deterministic_patch.py   # 确定性占位 patch (测试用)
├── compiler/                  # ★ 小说世界编译器 (TXT → WorldPackage)
│   ├── text_loader.py        # TXT 加载/清洗/章节/场景切分 (确定性)
│   ├── extractors.py         # LLM 抽取: 实体/关系/事件/规则
│   ├── scene_compiler.py     # 实体消歧 + 场景/章节编译 + 包构建
│   ├── book_compiler.py      # 全书消歧、多时间线与三级快照
│   ├── job_store.py          # SQLite 任务、租约、进度和调用预算
│   ├── job_runner.py         # 缓存、断点续跑、质量审核编排
│   ├── worker.py             # 独立编译 Worker
│   ├── benchmark.py          # 多小说 scan/estimate/enqueue/report
│   └── cli.py                # CLI 入口
├── examples/huarong_lane/     # 《第一狂妃》华容巷手工世界包
│   └── scenario.py           # (含 character_psyches 初始化)
├── web/                        # ★ FastAPI + Vue 3 Web 试玩界面
│   ├── app.py                 # /api/start、/api/turn、/api/state + 静态托管
│   ├── auth.py                # 账户、RBAC、令牌和审核审计
│   ├── run.py                 # Uvicorn 启动脚本
│   ├── stack.py               # Web + Worker 一键启停和健康检查
│   ├── frontend/              # Vue 3 + Vite 源码 (含 SaveManager)
│   └── static/                # 前端构建产物 (gitignore)
├── benchmarks/                # 多小说清单与版本化长轨迹回归库
├── contracts/                 # 核心 API v1 机器可读契约
├── worlds/                     # 编译生成的 WorldPackage JSON (产物 gitignore)
├── tests/
│   ├── unit/                 # 单元测试
│   │   ├── test_patch.py
│   │   ├── test_rules.py
│   │   ├── test_action_parser.py
│   │   ├── test_transition.py
│   │   ├── test_narrative.py
│   │   ├── test_character_agent.py   # ★ 角色 Agent + 调度器
│   │   └── test_compiler.py          # ★ 编译器
│   └── scenarios/            # 端到端场景测试
│       ├── test_turn_pipeline.py
│       ├── test_ai_turn.py
│       └── test_full_pipeline.py
├── docs/                      # 设计与实现进度文档
│   ├── plan.md
│   ├── AI快穿系统_模块作用功能技术对应表.md
│   ├── 实现进度.md
│   └── 架构图.png
├── novels/                    # 原著 TXT
├── .env / .env.example        # LLM 配置 (.env 被 gitignore)
├── start.cmd / stop.cmd        # Windows 一键启停
├── novelsim.ps1                # PowerShell 管理入口
├── pyproject.toml
└── .gitignore
```

---

## 4. 测试

**测试策略**：mock LLM 测试（确定性、CI 可跑、0 烧 token）为主，真实 LLM 测试用 `@pytest.mark.llm` 标记默认跳过，手动 `pytest -m llm` 运行。

当前验证结果：

- 本地确定性测试：`209 passed`。
- GitHub Actions：Pull Request 与 `main` 推送时并行执行 Python 3.8 回归和 Vue + Chromium E2E；强制使用 FTS5 且关闭真实 LLM 质量门禁，不产生模型费用。
- 真实 LLM 测试：13 个，默认跳过；反思语义审查、20 事件长轨迹评分、双章节 C 阶段编译，以及 D 阶段编译任务→评分→审核闭环已真实执行通过。
- PostgreSQL + pgvector 契约测试：1 个，默认跳过，需设置 `TEST_POSTGRES_URL`。
- Qdrant Local Mode 契约测试：1 个，真实执行通过，默认测试中按 marker 跳过。
- Vue 生产构建：`npm run build` 通过，27 个模块成功转换并输出到 `web/static/`。
- 新增旧库迁移、多存档管理、完整剧情恢复、删除级联及完整备份往返/损坏拒绝测试；存档弹窗、列表与改名流程已通过真实浏览器验收。

| 测试文件 | 用例数 | 覆盖 |
|---|---|---|
| `test_patch.py` | 9 | apply_patch 各 Operation 语义 + 纯函数不变性 |
| `test_rules.py` | 6 | 时空/物品/认知边界规则 |
| `test_action_parser.py` | 11 | JSON 提取 + 解析逻辑 + 真实 LLM |
| `test_transition.py` | 10 | patch_validator + Proposer 解析 + 真实 LLM |
| `test_narrative.py` | 11 | 一致性审查 + Generator 解析 + 真实 LLM |
| `test_turn_pipeline.py` | 5 | 端到端闭环 + 回放一致性 + 乐观锁 |
| `test_ai_turn.py` | 3 | AI Turn (mock + 真实) |
| `test_full_pipeline.py` | 3 | 完整 Pipeline (mock + 真实) |
| `test_character_agent.py` | 18 | ★ psyche 注入 + 长期记忆上下文 + Agent 决策 + 调度合并 + Pipeline 接入 + 真实 LLM |
| `test_compiler.py` | 22 | ★ text_loader + 两类章节标题 + 实体消歧 + 关系端点类型边界 + 场景/章节/单卷编译 + 跨章演化 + 真实 LLM 编译 |
| `test_book_compiler.py` | 4 | ★ 全书身份消歧、同名冲突、多时间线、三级快照和暂停边界 |
| `test_compilation_jobs.py` | 7 | ★ SQLite 状态机、Worker 租约、场景缓存、断点续跑、默认模型缓存键兼容、重启恢复、自动审核与真实 LLM 闭环 |
| `test_compiler_web_api.py` | 2 | ★ 编译任务创建/查询/控制 API 与小说目录安全边界 |
| `test_persistence.py` | 14 | ★ 重启恢复 + 原子提交 + 乐观锁 + 旧库迁移 + 备份往返 + 记忆检索/幂等/重建/裁剪 |
| `test_world_packages.py` | 12 | ★ 内置包只读 + 修订历史/差异/审核流 + 完整性校验 + 编译产物兼容 |
| `test_web_persistence.py` | 12 | ★ Web 回合/剧情恢复 + 长期记忆 + 存档管理 + 创作者修订/审核 API |
| `test_storage_backends.py` | 13 | ★ 后端工厂、DashScope密钥回退、批量/缓存 embedding、维度约束、pgvector Schema |
| `test_llm_config.py` | 2 | ★ DashScope密钥优先级与通用LLM_API_KEY兼容 |
| `test_qdrant_memory.py` | 11 | ★ Qdrant 点 ID、权重约束、作用域过滤、混合召回、陈旧点拒绝、FTS 降级、裁剪/删除同步与工厂配置 |
| `test_reflection_memory.py` | 9 | ★ 结构化反思、语义评分、证据链、幂等更新与认知冲突保护 |
| `test_llm_trajectory_eval.py` | 3 | ★ 长轨迹分块、聚合、五维评分、最低维度与高严重度问题门禁 |
| `test_memory_retrieval_eval.py` | 4 | ★ 50 查询中文基准完整性、角色隔离、MRR/nDCG 指标与基准入库 |
| `test_trajectory_eval.py` | 5 | ★ 20/60 回合门禁、版本断裂、死亡角色和多类型目标检测 |
| `test_postgres_persistence.py` | 1 | ★ 真实 PostgreSQL + pgvector 存储契约（环境驱动） |
| `test_qdrant_local.py` | 1 | ★ 真实 Qdrant Local Mode 写入、过滤与语义召回契约 |
| `test_auth_rbac.py` | 3 | ★ 密码/令牌、账户停用、稳定审计排序、creator/reviewer/publisher 权限隔离和审核审计 |
| `test_novel_benchmark.py` | 2 | ★ 多小说指纹扫描、跨平台换行一致性、全书排队和基准报告 |
| `test_trajectory_regression.py` | 1 | ★ 版本化 20/60 回合与故障注入回归库 |
| `test_api_contract.py` | 2 | ★ 核心 API v1 路径、方法和权威边界 |
| `test_stack.py` | 3 | ★ 一键启动器空状态、跨重启 PID 复用保护与真实 Web/Worker 启停 |
| `test_unity_contract.py` | 2 | ★ Unity 6.3 工程版本、API v1 路径和服务端权威状态消费 |
| **合计** | **209 本地通过 + 13 LLM/1 PostgreSQL/1 Qdrant 默认不运行 + 1 Playwright E2E** | **本地回归、RBAC、正式浏览器发布闭环、真实多小说烟测、Unity 契约、Qdrant 契约及召回门禁通过** |

运行：
```bash
.venv/Scripts/python.exe -m pytest                    # mock 测试 (默认)
.venv/Scripts/python.exe -m pytest -m llm -o addopts=  # 真实 LLM 测试
.venv/Scripts/python.exe -m pytest -m postgres -o addopts= # 真实 PostgreSQL
.venv/Scripts/python.exe -m pytest -m qdrant -o addopts= # 真实 Qdrant Local Mode
.venv/Scripts/python.exe -m engine.evaluate_memory_retrieval # 中文召回质量门禁
.venv/Scripts/python.exe -m engine.evaluate_llm_trajectory --session <id> # 真实长轨迹评分
cd web/frontend && npm run build                       # 前端生产构建
```

---

## 5. 真实效果示例

用户输入 → 系统完整产出（qwen3.6-plus 真实运行）：

**输入**：`"我冷冷地盯着夜清清，命令她把身上的外衫脱下来交给我，声音里没有一丝温度"`

**系统产出**：
- Action：`speak`（命令），目标夜清清
- 状态变化（4 条，每条带理由）：
  - `transfer_item`：夜清清迫于嫡庶尊卑与威压脱下外衫
  - `update_relation`：夜清清敌意加深（屈辱）
  - `update_relation`：夜清清对夜轻歌**忌惮加深**（打破"废柴"认知）
  - `set_attr`：衣衫不整状态缓解
- 旁白：完整古风小说段落（晨风/青石板/枯叶环境 + 屈辱/惊疑/忌惮心理 + 呼应嫡庶尊卑设定）
- 对白：夜轻歌（冰冷、不容置喙）"把外衫脱了，给我。" / 夜清清（隐忍、微颤）"……是，姐姐。"
- 系统提示：获得外衫 / 敌意上升 / 忌惮加深 / 身体状态更新

亮点：LLM 自主推演出**双向关系变化**（敌意 + 忌惮），"忌惮"完全是从"废柴嫡姐变强"语境读出来的，硬编码无法做到。

### 5.1 角色 Agent 自主行动（qwen3.6-plus 真实运行）

把夜清清从"被动受 patch"变成"主动决策者"。在受辱锚点唤醒她：

**夜清清的决策**：
- decided = `True`
- action：`observe`（隐于人群侧后方，冷眼旁观夜轻歌应对受辱局面的反应，暗中确认通奸陷害的局是否已按预期发酵）
- 情绪：冷静算计（强度 0.65）
- 感知：*"华容巷风波正起，夜轻歌此刻的狼狈正是推她入通奸死局的最佳跳板"*

亮点：夜清清没有莽撞反击，而是**根据自己"心机深/隐忍"的人设和"陷害上位"的目标**，选择冷眼旁观、确认阴谋是否得逞——这是从她的人格 + 目标 + 认知里推出来的，完全符合原著人设，而不是模板反应。调度器还能同时唤醒林管家（盘算重新站队）。

### 5.2 小说编译器：TXT → WorldPackage（qwen3.6-plus 真实运行）

`python -m compiler.cli novels/第一狂妃：废柴三小姐.txt -c 1` 编译第 1 章，LLM 自主抽出：

- **4 个角色**：夜轻歌（别名 三小姐/无名，标签 嫡系/废柴/丑女/佣兵）、夜清清（庶妹）、林管家、小王爷（未婚夫）
- **3 条关系**：夜清清→夜轻歌（庶妹-嫡姐）、夜轻歌→小王爷（未婚夫妻）
- **3 条世界规则**：家族名誉高于个人、丹田破碎者无法修炼、世家婚约有社会约束力

亮点：这与手工版 `examples/huarong_lane` **同构**——证明编译器确实能替代手工建模。实体消歧把"三小姐"和"夜轻歌"自动合并成同一角色（alias → 稳定 id），跨场景一致。

---

## 6. 对照 plan.md 的进度

| plan 章节 | 内容 | 状态 |
|---|---|---|
| 第三节 | 世界数据标准（Schema） | ✅ 完成 |
| 第四节 | 手工测试世界包（华容巷） | ✅ 完成 |
| 第五节 | 后端基础工程（仓库骨架 + venv + FastAPI 接入层） | ✅ 完成 |
| 第六节 | 权威状态与事件存储 | 🟡 SQLite 已验证；PostgreSQL 全契约与迁移已实现，待真实库演练 |
| 第七节 | 确定性规则引擎 | ✅ 完成 |
| 第八节 | 完整 Turn Pipeline | ✅ 完成 |
| 第九步 | 接入 LLM（任务 1/2/3） | ✅ 全部完成 |
| 第十步 | 角色 Agent（NPC 自主行动） | ✅ 完成（决策单元 + 调度器 + psyche schema + Pipeline 接入） |
| 第十一步 | 长期记忆（RAG） | ✅ 情景记忆、反思/语义记忆、证据链、认知冲突保护、重建、容量治理、FTS5/Qdrant 混合检索及 50 查询质量门禁已实现 |
| 第十二步 | 小说世界编译器（TXT→WorldPackage） | ✅ A/B/C/D 完成；全书任务化、全局身份、多时间线与三级快照已实现 |
| 第十三步 | 创作者后台 | 🟡 MVP+：账户/RBAC、审计、十类编辑区、关系图、修订差异、审核发布流、完整性校验与试玩 |
| 第十四步 | 多模态能力 | ⏳ 未开始 |
| 第十五步 | 测试体系（长轨迹/回归） | 🟡 版本化 20/60 回合库、真实 LLM 分块评分和正式浏览器 E2E 已完成；真实玩家轨迹样本待扩充 |

---

## 7. 技术栈现状

| 部分 | plan 建议 | 实际采用 | 说明 |
|---|---|---|---|
| 语言 | Python | Python 3.8.10 | 系统自带，pydantic 锁 1.x |
| Schema | Pydantic + JSON Schema | Pydantic 1.10.0 | ✅ 一致 |
| LLM SDK | — | openai 0.28.1 | 用 ChatCompletion API |
| LLM | 多模型 | qwen3.7-plus | DashScope OpenAI兼容端点；兼容自定义网关 |
| 配置 | — | python-dotenv + .env | key 不入库 |
| 后端 API | Python + FastAPI | FastAPI 0.99.1 + Uvicorn 0.29.0 | 提供会话、回合、状态和事件 API |
| Web 与创作者后台 | plan 原定 Next.js 创作者后台 | Vue 3 + Vite 5 | 玩家试玩闭环 + 创作者后台 MVP+ 已实现 |
| 测试 | pytest | pytest 7.4.4 | ✅ 一致 |
| 数据库 | PostgreSQL + JSONB | 当前 SQLite 3.35.5；PostgreSQL JSONB 代码保留 | SQLite 为当前权威存储，PostgreSQL 暂不启用 |
| 记忆检索 | pgvector | SQLite FTS5 + Qdrant Local Mode 混合召回 | Hit@4 1.000、MRR 0.957、nDCG@4 0.967；权重 80/10/5/5 |
| Agent 编排 | LangGraph | **未接入** | 当前用纯 Python 流程 |
| 3D 客户端 | Unity 6.3 | Unity 6000.3.15f1 + URP 17.3 依赖骨架 | **竖切片进行中**：第三人称移动、NPC 交互、API v1 和剧情 HUD 已落地；待渲染管线资产、Editor 编译和 Play Mode 验收 |

---

## 8. 过程中解决的环境问题

记录踩过的坑，便于环境复现：

1. **pip 21.1.1 SSL bug**：`check_hostname requires server_hostname`。绕过方案：用 `urllib` 直接下 wheel 到 `.deps/`，再 `pip install --no-index --find-links=.deps` 离线装。
2. **Python 3.8 依赖版本**：pydantic 锁 1.x、numpy 1.24、pandas 1.5、openai 0.28——这些是最后兼容 3.8 的版本组合。
3. **系统代理 TLS 失败**：Clash 代理 `127.0.0.1:7897` 的 TLS 握手挂掉（`SSLEOFError`），requests 自动读它。解决：脚本开头设 `NO_PROXY=*` 直连。`.env` 里也配了。
4. **openai 新旧 SDK 混用**：原 `api_test.py` 用了新版（≥1.0）的 `openai.chat.completions` 风格 + 旧版全局变量设置，不兼容。统一用 0.28 的 `ChatCompletion.create`。

依赖 wheel 缓存在 `.deps/`（已 gitignore），环境迁移时可直接复用。

---

## 9. 下一步候选方向

核心 AI 引擎 + 角色 Agent + 编译器已完整，当前优先级如下：

| 方向 | plan 对应 | 价值 | 难度 |
|---|---|---|---|
| Qdrant 基准扩充 | 第十一步 | 从真实长轨迹加入多答案、否定、时间变化和困难负样本 | 中 |
| 编译器真实全书基准 | 第十二步 | quick 已收口；根据修复结果决定是否进入 stress，不直接启动 full | 高 |
| 多租户与云同步 | 第十三步 | 在现有账户/RBAC 之上隔离团队、项目和玩家云存档 | 中-高 |
| Web 体验与测试 | — | 玩家世界包选择页、扩充移动端与失败恢复 E2E | 中 |
| 长轨迹基准库 | 第十五步 | 收集真实存档并建立目标一致性、重复率和剧情推进回归阈值 | 中 |
| 角色 Agent 进阶 | 第八步 | 多分支目标树、NPC 之间连锁互动、LangGraph 编排 | 中-高 |

### 9.1 当前生产演练

已建立 [`真实全书编译生产演练.md`](./真实全书编译生产演练.md)，新增：

- quick / stress / full 三档真实编译；
- 执行前场景、调用量、耗时和可选费用估算；
- SQLite 跨 Worker/重启的 LLM 硬调用预算；
- 预算耗尽安全暂停并保留缓存；
- JSON + Markdown 运行报告。

首轮两书 quick 演练已于2026-07-28收口，共40章、49场景：

- 《第一狂妃：废柴三小姐》30/31次调用，缓存恢复24/24场景命中，质量分0.896，
  阻断问题0，世界包进入`pending_review`；
- 《第一狂妃：绝色邪王宠妻无度》32/33次调用，质量分0.826，检测到2个高严重度
  世界观/目标问题，世界包按新策略退回`draft`；
- 双书任务均为`completed`，运行报告`completed=true`；暂停、断网恢复、租约释放、
  调用预算、审计和审核状态流均已真实验证。

### 9.2 Beta 启动与 CI

已建立 [`Beta一键启动与CI.md`](./Beta一键启动与CI.md)：

- Windows `start.cmd` 双击启动与 `novelsim.ps1` start/status/restart/stop；
- Python 跨平台 `python -m web.stack` 入口；
- 精确 PID、日志、端口冲突保护、健康检查和进程树回收；
- GitHub Actions Python 3.8 确定性门禁；
- Linux/Windows 双平台 Playwright 启动路径；
- Vue 构建与 Chromium RBAC E2E 门禁。

### 9.3 紧接着要做

1. 创建并合并真实全书生产演练与 Beta CI 里程碑 PR；
2. 修复第二本的现代特工目标残留和长期目标停滞，再提交人工审核；
3. 为结构化目标增加世界/时间线作用域和生命周期终止规则；
4. 完成修复后再决定是否进入200章`stress`，不直接启动高成本`full`；
5. 补充真实玩家长轨迹样本、前端失败恢复E2E和团队/项目级租户边界。
